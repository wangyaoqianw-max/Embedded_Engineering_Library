# AGENTS.md

## 基本原则

- 默认使用中文沟通和编写仓库说明。
- 保持改动范围最小，只处理用户明确要求的内容。
- 未经明确要求，不修改、删除、格式化或重构资产源码。
- 修改前先阅读根目录及目标资产目录中的 `README.md`，确认分类、来源和使用边界。
- 无法验证的信息必须标为“未验证”或“待确认”，不得猜测。

## 目录与分类

- `original/` 保存独立编写的原创资产。
- `adapted/` 保存基于外部代码实质改编、裁剪、移植或封装的资产。
- `third_party/` 保存原样或仅经配置级调整的第三方资产。
- 每项资产必须拥有独立目录和自己的 `README.md`，不得把资产文件散放在分区根目录。
- 无法确定归属时，按第三方资产处理或暂停收录。

## 第三方代码边界

- `third_party/` 中的源码默认只读；更新说明文件不等于获得修改源码的授权。
- 修改第三方源码前必须得到用户明确授权，并在资产说明中记录修改内容。
- 保留上游目录结构、版权声明和许可证，不主动重构或统一格式。
- 不得把第三方代码或轻量配置改动描述为原创代码。

## 新增与迁移资产

新增或迁移资产时必须：

1. 检查仓库中是否已有重复或功能重叠的资产。
2. 确认资产分类、来源、版本和许可证。
3. 创建独立资产目录。
4. 编写或更新资产 `README.md`。
5. 同步更新根 `README.md` 的资产索引。
6. 执行与改动范围相称的验证并报告未验证项。

资产 `README.md` 至少应说明：资产简介、来源与版本、许可证、目录内容、依赖与适用环境、使用方式、适用边界、已知限制、验证情况、修改记录和复用性评审结论。

## 评审要求

评审资产时至少检查：

- 是否具有明确、可重复的使用场景。
- 对芯片、编译器、操作系统、HAL、构建系统和硬件资源的耦合。
- 接口是否清晰，集成成本是否可控。
- Flash、RAM、栈、堆、实时性等资源约束。
- 线程、中断、DMA、缓存和并发环境下的限制。
- 许可证、版权、来源和修改记录是否完整。
- 已验证的平台、方法和范围，以及尚未验证的部分。

不能仅凭“能够编译”认定资产可复用。评审结论应区分事实、推断和未验证项。

## 安全边界

- 禁止提交密钥、Token、证书私钥、个人隐私、公司内部代码或其他保密材料。
- 发现疑似敏感信息时停止写入并提示用户。
- 涉及删除、覆盖、大规模移动或外部写入时，必须确认目标和授权范围。
- 不在输出、日志或文档中暴露敏感凭证。

## 验证与交付

- 文档改动检查链接、编码、目录一致性和 Git 差异。
- 源码改动执行目标资产已有的构建或测试流程；没有可用流程时明确说明。
- 不擅自提交、推送或发布变更。
- 交付时概述实际改动、验证结果和遗留风险。
